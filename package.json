{
  "name": "dawsome",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@tauri-apps/api": "^1.0.0-rc.3",
    "@types/node": "^16.11.26",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "info": "tauri info",
    "build": "tauri build",
    "dev": "tauri dev"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.0-rc.8",
    "turbo": "^1.1.10"
  }
}
